# Runtime deps for the standalone OMR correction service.
# Includes the OMRChecker core deps (not listed anywhere else in this repo,
# see plan notes) plus the FastAPI/PDF/OCR-support deps used by this service.

# --- FastAPI service ---
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1

# --- OMRChecker core (src/) ---
opencv-python-headless>=4.9,<5
numpy>=1.25.0,<2
pandas>=2.0.2,<3
jsonschema>=4.17.3,<5
dotmap>=1.3.30,<2
deepmerge>=1.1.0,<2
rich>=13.4.2,<14
screeninfo>=0.8.1,<1
matplotlib>=3.7.1,<4

# --- Answer sheet generation / scan ingestion ---
reportlab>=4.2,<5
Pillow>=10.4,<13
PyMuPDF>=1.24,<2
qrcode[pil]>=7.4,<8
